{
    executable: {{{ROOT_DIR}}}/bin/tezpeak
    args: [
        {{#configuration.STARTUP_ARGS__CLI_ARGS}}
        {{.}}
        {{/configuration.STARTUP_ARGS__CLI_ARGS}}
    ]
    environment: {
        HOME: {{{ROOT_DIR}}}
    }
    working_directory: {{{ROOT_DIR}}}/data
    restart: always
    restart_delay: 20
}